version: "3.8"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "5000:5000"
    volumes:
      - backend_data:/app/data
    environment:
      - NODE_ENV=production
    networks:
      - app_network

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    environment:
      - REACT_APP_API_URL=http://<13.61.177.172>:5000
    networks:
      - app_network

volumes:
  backend_data:

networks:
  app_network:
    driver: bridge
